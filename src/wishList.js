import { Car } from "./src/car.js";

const myCar = new Car("Lexus", "is250", "2005");

export class WishList {
  add(car) {
    document.createElement;
    let wishList = document.getElementById("WishList");
    wishList.appendChild(car);
  }
}
